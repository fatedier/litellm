# Fixtures

Store reusable E2E seed data, request payload templates, and environment
overrides here.

Keep secrets out of this directory. Reference them through CI or deployment
environment variables.
